
## MySQL:

SELECT DISTINCT CITY 
 FROM STATION 
 WHERE CITY REGEXP "[a,e,i,o,u]$" ;   Here the $ symbol is the difference.



## MS SQL SERVER:
 
 SELECT DISTINCT CITY 
 FROM STATION 
 WHERE CITY LIKE "%[a,e,i,o,u]" ;  Here the position of the % symbol decides the ending /starting of words
