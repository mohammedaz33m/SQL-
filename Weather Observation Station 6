## MySQL:

SELECT DISTINCT CITY 
 FROM STATION 
 WHERE CITY REGEXP "^[a,e,i,o,u]" GROUP BY CITY;
 
 ## MS SQL SERVER:
 
 SELECT DISTINCT CITY 
 FROM STATION 
 WHERE CITY LIKE "[a,e,i,o,u]%" GROUP BY CITY;
